class Cell():
     def __init__(self, x, y, value):
         self.x = x
         self.y = y
         self.value = value ? WALL

class MazeGenerator(info):
    def __init__(self, info):
        self._width = info.width
        self._height = info.height
        self._algorithm = info.algorithm
        self._seed = info.seed
        self._maze: List[Cell]
        here I just need to initialize the class, not to parse a config.txt, but if the config is correct i have all the info to create my maze, which leads to a simpler class that can be reused for other use cases with another parser or even hardcoding values     

def dfs():
    initialPos = info.entry;
    visited
for (i in range(0, maze.width))
    if (maze.cells[i][j].isVisited)
        